service : {
  checkGuess: (text) -> (record {
                           correct: bool;
                           feedback: text;
                         }) query;
  getCurrentWord: () -> (text) query;
  startGame: () -> (text);
}
